version: "2.1"

services:
  weatherforecast-web:
    image: weatherforecast-web
    build:
      context: .
      dockerfile: WeatherForecast.Web/Dockerfile
  mssql:
    image: mcr.microsoft.com/mssql/server:2017-latest-ubuntu
    environment:
      - ACCEPT_EULA=Y